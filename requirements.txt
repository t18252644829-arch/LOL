# OCR 引擎(跑在你自己电脑上)。解析层与测试不需要这些。
# RapidOCR:自带中文模型、离线运行。注意:它需要 Python 3.8~3.12,
# 太新的 Python(3.13+)暂不支持,请用 Python 3.12 运行。
rapidocr-onnxruntime
# 备选:若想用 PaddleOCR,取消下面两行注释(安装更重、更易踩坑)
# paddleocr>=2.7
# paddlepaddle>=2.5
