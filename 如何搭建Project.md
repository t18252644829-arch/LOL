# 如何把「私人 LOL 教练」搭成一个 Project

目标:在对话平台(以 Claude.ai 为例)建一个 Project,让里面每个新对话都自动是你
的教练、记得规矩、会联网跟版本。本地工具负责产数据,Project 负责当教练。

> 前提:Projects 通常是**付费功能**(如 Claude Pro/Max)。另外**必须能联网搜索**,
> 否则"跟进版本"做不到。

---

## 一、建 Project

1. 打开 claude.ai(或桌面/手机 App)。
2. 左侧边栏找到 **Projects**(项目)→ **Create project**(新建)。
3. 命名,比如 **「LOL 私人教练」**。

## 二、写入「教练准则」(最关键一步)

1. 进入这个 Project,找到 **自定义指令 / Instructions / 项目说明**。
2. 打开仓库里的 `教练准则.md`,把其中**【 】之间的整段内容**复制进去,保存。
   - 这段就是教练的"大脑设定":开场读状态包、强制联网确认版本、回合工作流、
     任务命令格式、数据字段说明、诚实原则、风格。

## 三、确认联网搜索已开启

1. 在设置 / 功能偏好里,确认开启 **网页搜索 / Web search**。
2. 验证:在该 Project 新开一个对话,问一句"现在 LOL 最新版本号是多少?"——
   它若能给出当前版本(并附来源),说明联网正常。**给不出 / 说不知道 = 没联网,
   要先解决,否则跟不了版本。**

## 四、上传知识文件(可选但推荐)

在 Project 的 **知识 / Add content** 里上传(作为长期背景):

- `data/profile.json` —— 你的个人档案(分路/段位/目标/英雄池/计划)
- 你的当前计划文本

> 说明:每次对话你还会用 `教练简报.bat` 贴"状态包"(已含档案+任务+最近数据),
> 那是**实时**通道;知识文件是**长期备份**。档案变化大了就重传一份。

## 五、每次训练怎么用

1. 本地双击 **`教练简报.bat`** → 复制导出的「状态包」JSON。
2. 在该 Project **新开一个对话**,第一句话用下面模板:

```
我来训练。这是我的最新状态包,请先联网确认当前版本,再核对我的任务、给建议、布置新任务。
<把状态包JSON粘在这里>
```

3. 教练会:核对上次任务 → 结合当前版本/meta 给建议 → 给你可粘贴的 `tasks add`
   命令。你把命令在 PowerShell 执行,存下新任务。
4. 打下一批 → 截图 → `批量识别.bat` → 回到第 1 步。

---

## 三者分工(记住这个)

- **本地工具(bat/脚本)= 手脚**:产数据、存档案/任务。在你电脑上跑。
- **Project(准则+联网)= 大脑**:读数据、搜版本、分析、布置任务。
- **你 = 桥梁**:跑 bat、贴状态包、执行命令。

要改"大脑设定"(加指标/改风格/加英雄规则),就回来找 code 模型改 `教练准则.md`,
再把新内容更新到 Project 自定义指令即可。
